import authInterceptor from './authInterceptor.js';
import exitfxService from './exitfxService.js';
export default angular
    .module( 'app.factory', [] )
    .factory( 'authInterceptor', authInterceptor )
    .factory( 'authInterceptor', authInterceptor )
    .factory( 'exitfxService', exitfxService)
    .name;